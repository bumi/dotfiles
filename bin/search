#!/bin/sh
# https://github.com/pengwynn/dotfiles/blob/master/bin/search
# Quick search in a directory for a string ($1).
#
set -e

# use -iru to search directories ack usually ignores (like .git)
ack -i $1

